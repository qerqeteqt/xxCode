# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/

# ---- 虚拟环境 ----
# 本项目使用 conda 环境 langgraph，不在此目录内，这里只是防止误提交
.venv/
venv/
env/

# ---- IDE ----
.idea/
.vscode/

# ---- 密钥 ----
# .env 存放真实 API Key，永不入库
.env
.env.local
.env.*.local

# ---- Agent Runtime 运行数据 ----
# sessions 是运行时产生的对话流水，属于本机数据，不入库
.agent/sessions/
# 整理状态和备份也要看本机情况，换台机器就重来，没必要入库
.agent/consolidation.json
.agent/memory-backups/
# 注意：.agent/memory/ 下的 Markdown 是长期知识，需要入库，故不在此忽略

# ---- 一次性的实验产物 ----
# 贪吃蛇是拿这个 agent 跑出来的东西，不是这个项目的代码。
# 留在磁盘上自己玩，但不入库 —— 它混在 tests/ 里会被 pytest 当测试收集，
# 语义上不对。真要长期留，应该挪到 examples/ 之类的地方。
tests/snake.py
tests/test_snake.py
